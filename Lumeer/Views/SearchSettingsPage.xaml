<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Lumeer.ViewModels"
             x:Class="Lumeer.Views.SearchSettingsPage">
    <ContentPage.BindingContext>
        <viewModels:SearchSettingsViewModel/>
    </ContentPage.BindingContext>
    
    <ContentPage.Content>
        <StackLayout>
            <StackLayout>
                <TableView Intent="Form">
                    <TableRoot>
                        <TableSection Title="Search Settings">
                            <SwitchCell Text="My tasks only" On="true" />
                        </TableSection>
                    </TableRoot>
                </TableView>
            </StackLayout>
            <Button Text="Ok"
                    Command="{Binding OkCmd}"
                    VerticalOptions="End"
                    HorizontalOptions="Center"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>